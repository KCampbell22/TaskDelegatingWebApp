@model IEnumerable<TaskDelegatingWebApp.Models.TaskItem>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TaskName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AssignedPerson)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AssignedDay)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TaskName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AssignedPerson.PersonId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AssignedDay.DayId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TaskItemId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TaskItemId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TaskItemId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
